// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
};
export const baseURL = 'localhost:8080/api/profile/';
export const profile = '1';
export const apiResource = {
  header: '/header',
  home: '/home',
  about: '/about',
  skills: '/skills',
  qualifications: '/qualifications',
  projects: '/projects',
  blogs: '/blogs',
  contact: '/contact',
  footer: '/footer'
};
/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.


// api/profile/1/header
// api/profile/1/home
// api/profile/1/about
// api/profile/1/skills
// api/profile/1/qualifications
// api/profile/1/projects
// api/profile/1/blogs
// api/profile/1/contact
// api/profile/1/footer

